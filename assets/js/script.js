// creates to random numbers between 0 and 1

function getRandomInt(max) {
    return Math.floor(Math.random() * max);
  }
  
  console.log(getRandomInt(2));
  console.log(getRandomInt(1));
  console.log(Math.random());